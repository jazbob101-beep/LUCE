# Tomi GPS and `glgps` Reference

## Scope

This reference consolidates what the preserved Tomi evidence establishes about the GPS software stack, its Linux-facing interfaces, observed runtime, and the limits of identifying the physical receiver. It is not a GPS hardware teardown or a claim that any historical filesystem or OpenTom source tree exactly matches the current installed device.

Evidence labels used here:

- **ESTABLISHED_TT3** — directly supported by Tomi-specific captured or live evidence.
- **LIVE_OBSERVED** — present in the dated Aug. 24, 2026 Tomi terminal capture; that capture is a terminal transcript, not a raw UART trace.
- **EXACT_BINARY** — static finding tied to the listed hash-identified executable.
- **SOURCE_CORRELATED** — defined by the inspected Austin/type-42 OpenTom source, not proven to be the built or running kernel.
- **PROTOCOL_CORRELATED** — format/output evidence from `glgps` or its downstream log, not proof of receiver silicon.
- **COMPARATIVE_ONLY** — another TomTom device or binary; not transferable as Tomi fact.
- **UNRESOLVED** — the preserved evidence does not decide the question.

## Evidence and specimens

| Specimen | Identity and provenance | What it supports |
|---|---|---|
| Historical TT3 filesystem `glgps` | 1,090,108 bytes; SHA-256 `c807db452a5a84b48988e30034910a201e73cf2c86ff9252287782e919dcd4b7`; `/mnt/d/Codex/TT3/TT3-capture-20260804-163938/filesystem-copy/bin/glgps`. ARM ELF32 little-endian, dynamically linked, stripped; interpreter `/lib/ld-linux.so.2`, dependencies `libm.so.6` and `libc.so.6`. Embedded GLL 2.15.0 build 65947, dated 2009-08-12. | **EXACT_BINARY:** the historical captured executable and the Aug. 19 epoch analysis. It is not established as the later running binary. |
| Later `glgps` forensic specimen | 1,176,168 bytes; SHA-256 `45fcfa0ae50fb915ba536feda712769324c2ea25ab16acc01f3d0ce9048dd099`; `/mnt/d/Codex/TT3/glgps-forensics/glgps`. ARM ELF32 little-endian, stripped; interpreter `/lib/ld-linux.so.2`, dependencies `libm.so.6` and `libc.so.6`. Embedded GLL 2.16.201 build 354431, dated 2018-10-08. | **EXACT_BINARY:** static `FIN,1` / ASIC-dead shutdown and watchdog analysis. It contains RTC-related strings including `/dev/rtc0` and `glrtc-rtc-file`; strings alone do not prove those paths execute. |
| TT1 comparative donor | 1,176,156 bytes; SHA-256 `25aff8fd28bdf77663ce756b385fa91ee8c141e19423d04e36939e3e7b9e9278`; `/mnt/d/Codex/TT3/gps-epoch-analysis-20260819/evidence/glgps.comparative-tt1-donor`. GLL 2.16.201, build 77382, dated 2010-06-08. | **COMPARATIVE_ONLY:** has the fully analyzed `GlRtc*` provider and private RTC ioctl path. It is neither the 2018 binary nor Tomi evidence. |

The historical TT3 2.15.0 file and later 2.16.201 forensic file differ in size, hash, and embedded build identity. The Aug. 24 live transcript reports `/mnt/sdcard/bin/glgps`, file size 1,176,168 bytes, and runtime `$PGLOR,RID,BCD,2,16,201*76`, which correlate strongly with the 2018 specimen. **No on-device SHA-256 was recorded**, so the live process and preserved 2018 bytes are not cryptographically proven identical. Do not call either preserved file “the current deployed binary” without a new identity record.

## What is known about the physical GPS hardware

**The physical receiver package/chip remains unresolved.** Tomi’s captured `ttgo.bif` contains `GPSFirmwareVersion=2.15.0 65947` (**DEVICE_DIAGNOSTIC / firmware string**). The Austin/type-42 OpenTom profile selects numeric GPS type 130, named `GOGPS_GL_BCM4750` / “Global Locate Barracuda” (**SOURCE_PROFILE**). The Aug. 24 runtime state also reports `receiver_gpstype=130` and “Barracuda” (**LIVE_OBSERVED software label**).

These facts identify software/profile labels and a firmware-version string, not the physical silicon/package. The 2018 GLL 2.16.201 runtime label is generated by GLL and is not a GPS-module firmware response. No direct physical inspection, package marking, board-level tracing, or device diagnostic uniquely identifies the receiver. No TT4 or TJ hardware identity is imported here.

## Linux / platform integration

The Tomi kernel log and profile identify the S3C2412/Austin software platform. In the inspected OpenTom working source, the type-42 profile (`src/linux-s3c24xx/arch/arm/mach-s3c2410/tomtomgo-ioaustin.h`) sets `gpsdev="ttySAC1"`, GPS type 130, `GPS_RESET=GPC5` (inverted), `GPS_ON=GPF4`, `GPS_POWERON_OUT=GPF3`, GPS TX/RX on GPH4/GPH5, and CTS/RTS on GPG9/GPG10. These are **SOURCE_CORRELATED profile definitions**, not live GPIO electrical measurements or proof of the exact kernel image running in the Aug. 24 session. The working OpenTom checkout was dirty; its inspected HEAD was `eb3d2037315bde5da2875b38cba4efba9d47eac7`, and the relevant source files were not established as tracked blobs at that HEAD.

The source driver `src/linux-s3c24xx/drivers/barcelona/gps/gps.c` registers `/dev/gps` with major 243. Its `IOW_GPS_ON` and `IOW_GPS_OFF` ioctl cases explicitly log and ignore those requests; `IOW_GPS_RESET` invokes the source reset helper. The driver also exposes a reset control path and GPS timestamp/PPS-related logic. In the Barracuda profile path, source initializes platform pins/UART and GPS power; the code comments that it normally avoids resetting Barracuda on every start, but source contains a first-start reset branch. This does not establish what occurred in the observed boot. The header `include/barcelona/Barc_gps.h` defines the ioctl names and device major.

No GPS-facing physical MMIO base/address or live GPIO level was validated by the Tomi GPS investigations. GPIO names above are profile-level mappings. Do not import TJ/Atlas III MMIO addresses. The inspected source is correlation only; it is not a frozen exact build input or proof of live kernel correspondence.

## `glgps`

### Startup and runtime model

In the Aug. 24 session, a custom Tomi-side script `/mnt/sdcard/opentom/thomas/gps/start-gps.sh` set `PATH` and `LD_LIBRARY_PATH=/mnt/sdcard/opentom/lib`, linked `/dev/gpsdata` to `/dev/ttySAC1`, created `/var/run/gpspipe`, started a separate `ttgpsd`, then launched `/mnt/sdcard/bin/glgps normal` and a separate `time-sync.sh`. The runtime log says `glgps` read `/mnt/sdcard/gl/cfg/glconfig.xml`, opened `/dev/gpsdata`, found `/proc/barcelona`, and opened `/dev/gps`.

This establishes one **LIVE_OBSERVED custom-run topology**, not the default TomTom navigation launch path or every current boot. The historical extracted `/etc/rc.glgps` instead selects `/mnt/sdcard/bin/glgps` when present (otherwise `/bin/glgps`) and launches `glgps normal` in the preserved image. Its hash is `4ae11ef1c6b64a41d18158c2e05977d7c346831da6f7cc66bc9fef3c834e2a44`. The round-trip package preserves that historical script byte-identically; it does not prove it is installed in the later custom root.

### Device interfaces and data flow

```text
GPS receiver / serial endpoint (physical protocol not captured)
    ↕  S3C UART endpoint reported as /dev/ttySAC1
    ↕  Aug. 24 symlink /dev/gpsdata -> /dev/ttySAC1
glgps normal
    ├─ control/timestamp/platform interface: /dev/gps, /proc/barcelona
    ├─ GLL text output including NMEA sentences and $PGLOR extensions
    ↓  /var/run/gpspipe (named FIFO in the custom run)
ttgpsd (separate consumer)
    └─ custom state/satellite/raw text files under /var/run
```

The transcript directly observes the symlink, device nodes, process command lines, FIFO and output files. It does **not** capture raw bytes at the UART, so the arrows describe the observed software endpoint and downstream flow, not a byte-level proof of every receiver exchange. `/dev/gps` is a TomTom platform control/timestamp device, not the serial stream. `ttgpsd` was a custom separate consumer; no evidence here establishes that stock PNDNavigator consumed these custom output files.

The Aug. 24 capture reports 72,330 valid parsed sentences, with no checksum or malformed-sentence errors before shutdown. Its last ordinary RMC carried UTC date `240826` (2026-08-24) and a valid status. Exact coordinates are intentionally omitted. No raw UART capture, receiver binary-message dump, socket/TCP GPS interface, or independently confirmed baud/protocol negotiation is present. The historical `glconfig.xml` specifies port `/dev/gpsdata`, baud 115200, and `lto.dat`; that is extracted configuration, not proof of the exact live custom-run config contents or receiver-side wire settings.

### Shutdown behavior from the later binary

For the 2018 specimen only, static analysis establishes that `glgps` formatted `$PGLOR,FIN,1*36` itself. FIN reason 1 maps to `ASIC is DEAD`; ordinary control-FIFO quit maps to FIN 0. The observed transcript records the final FIN 1 at uptime 7,393,220 ms, about 360 ms after the last normal batch, while `ttgpsd` survived with the stream open and no EOF/reopen or parser errors. The 2018 binary’s ASIC-dead path uses one-shot ASIC-init/MEIF transaction watchdogs (normally 1500 internal engine ticks, plus a separate 75-tick early-init path); the tick-to-time conversion and the specific expired site are unknown.

The custom `time-sync.sh` log showed nine successful GPS-to-system-to-RTC sync entries in the same second as shutdown. This is a temporal precursor, not proof of causation. The evidence attributes the termination mechanism to `glgps`’s internal ASIC-dead classification; the lower-level trigger remains unknown. The kernel source’s ignored `IOW_GPS_OFF` behavior means that final ioctl/log is cleanup, not proof that Linux powered the physical receiver off. No direct USB/PPP/UART-driver cause was established.

## GPS protocol and firmware findings

**PROTOCOL_CORRELATED:** captured downstream text contains standard NMEA-style GGA/GSA/GSV/RMC sentences and GLL-generated `$PGLOR` extensions such as RID and FIN. `$PGLOR` is produced by the TomTom/Broadcom GLL process, not established as receiver-originated UART text. The raw serial wire protocol remains **UNRESOLVED**: no raw UART bytes, SiRF binary messages, receiver message IDs, command/response sequence, checksum, or firmware-query exchange were captured. A GLL software label or Barracuda profile cannot identify the physical chipset.

Version evidence has distinct meanings:

- `ttgo.bif`: `GPSFirmwareVersion=2.15.0 65947` in the 2026-08-04 captured filesystem (**DEVICE_DIAGNOSTIC**, file-level identity).
- Historical 1,090,108-byte executable: GLL 2.15.0 build 65947 (2009) (**EXACT_BINARY**).
- Aug. 24 live GLL RID: `2.16.201` (**LIVE_OBSERVED GLL software identity**, not GPS firmware identity).
- Preserved 2018 executable: GLL 2.16.201 build 354431 (**EXACT_BINARY**, strongly correlated with but not cryptographically tied to the live process).
- 2010 TT1 donor: GLL 2.16.201 build 77382 (**COMPARATIVE_ONLY**; a third distinct specimen).

## Reset / power / lifecycle

The Austin profile and `gps.c` source establish a platform-controlled reset/power design, but no live electrical level, reset pulse, antenna/LNA state, or measured time-to-fix is established. The 2009 binary-specific epoch work used a historical test in which setting Linux time before start and a cold-start job still produced a 2007 date; that conclusion belongs to that run and that hash-identified executable only.

The Aug. 24 run is materially different: it reports GLL 2.16.201 and a valid current 2026 RMC date. The captured run does not isolate whether the difference arose from GLL build, configuration, time-sync behavior, receiver state, or another variable. Accordingly, the old epoch diagnosis is **scoped to the verified 2009 specimen and test**; the broad claim that the current Tomi `glgps` lacks an RTC provider is superseded/overbroad. The 2018 specimen contains RTC provider-related strings, but string presence does not prove provider execution. No controlled, hash-bound comparison resolves the cause of the differing dates.

## Assistance / ephemeris data

The historical 2009 executable contains TT ephemeris ARP and Custom Request Manager plumbing, as well as GLL internal assistance-time interfaces; the analyzed build did not contain the `GlRtc*` provider, `/dev/rtc0`, `glrtc-rtc-file`, or its private RTC ioctl constants. The separate 2010 TT1 donor does contain a detailed RTC provider; it is comparative only. The 2018 Tomi forensic specimen has strings for `/dev/rtc0` and `glrtc-rtc-file`, but there is no live syscall trace proving those paths ran.

The captured filesystem includes `ephem/ee_meta.txt` with `Expiry=1329067832`, and QuickGPS/`lto.dat` references in historical configuration/startup material. Classify these as **ASSISTANCE_DATA / HISTORICAL_ARTIFACT**, not proof that a current QuickGPS update occurred, that the dated file was used by a particular run, or that the physical receiver accepted it. Map and navigation data are outside this reference.

## Comparative findings

- **SAME_PLATFORM_CORRELATION:** OpenTom’s Austin/type-42 profile and Tomi’s captured type-42/Austin identification align at the software/platform level. Exact source-to-running-kernel correspondence remains unproven.
- **SAME_SOFTWARE_STACK / COMPARATIVE_ONLY:** TT1 GLL 2.16.201 donor contains the RTC adapter missing from the verified 2009 TT3 executable. Its binary routines, addresses, and ioctl contract are not Tomi facts.
- **CROSS_FAMILY_COMPARISON / NON_TRANSFERABLE:** TJ Atlas III uses a different SiRF/AT4X0A family and has separate GPS binaries and MMIO mappings. No TJ GPS register, chip identity, or protocol conclusion is applied to Tomi.
- Other device-level GPS packages (including ThomasTouch/Nx GPS) concern navigation/application consumers, not proof of Tomi’s physical receiver or core `glgps` implementation.

## Negative findings

- No direct physical GPS package identification or live board-level pin measurement.
- No raw Tomi UART capture; therefore no established SiRF-binary or other receiver-wire protocol, binary message IDs, or wire-level checksum finding.
- No on-device digest binding the Aug. 24 running `/mnt/sdcard/bin/glgps` to the preserved 2018 specimen.
- No evidence that the TT1 donor’s private RTC provider or addresses ran on Tomi.
- No live MMIO validation of the Austin source-profile GPS reset/power mappings.
- No demonstrated GPS consumer relationship from the custom `ttgpsd` output files to stock navigation software.
- No proven causal link between the rapid time-sync loop and `FIN,1`.

## Known unknowns

Physical receiver make/package and firmware; exact serial wire protocol and baud at the receiver; hash of the Aug. 24 executable and its exact live config; whether the 2018 RTC provider executed; which component selected the old epoch in the 2009 run; why 2009 output was 2007 while the later run produced a current date; exact trigger for the ASIC-dead watchdog; live GPIO/MMIO/reset/power behavior; and which application consumed each GPS output remain unresolved.

## Canonical references

- [TT3 Tomi device profile](../devices/tt3-tomi.md) — stable device and software-platform identity; intentionally leaves physical GPS package unresolved.
- [Tomi boot chain](../architecture/tomi-boot-chain.md) — Linux image and boot boundary, not GPS runtime details.
- [Tomi Runtime ABI](tomi-runtime-abi.md) — command compatibility only.
- [Evidence Index](../../evidence/INDEX.md) — selected durable evidence anchors.

## Provenance

The external reconciliation report for this migration is `/mnt/d/Codex/TT3/luce-bootstrap-tomi-gps-glgps-reconciliation-2026-09-24.md`. It inventories GPS-related work units, exact specimen hashes, source and transcript paths, investigation dispositions, and unresolved provenance boundaries. Raw captures, binaries, and analysis tooling remain in their existing evidence locations; none were moved into LUCE.
